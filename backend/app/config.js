//This configuration file will hold global constants that define the default behavior of how the concept of generations will work, amongst other global things.
const SECONDS = 1000;
const MINUTES = SECONDS * 60;
const HOURS = MINUTES * 60;
const DAYS = HOURS * 24;

const REFRESH_RATE = 5;
const STARTING_BALANCE = 50;
module.exports = {
  SECONDS,
  MINUTES,
  HOURS,
  DAYS,
  REFRESH_RATE,
  STARTING_BALANCE,
};
